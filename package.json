{
  "name": "control-decks",
  "version": "0.1.0",
  "private": true,
  "description": "Experimental HACP Games, Decks, and Cards.",
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
