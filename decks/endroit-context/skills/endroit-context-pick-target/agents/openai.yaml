interface:
  display_name: "Endroit Context · Pick Target Context"
  short_description: "Select one known Endroit Target and load only its current inspe"
  default_prompt: "Use $endroit-context:endroit-context-pick-target on the current working object."
policy:
  allow_implicit_invocation: false
