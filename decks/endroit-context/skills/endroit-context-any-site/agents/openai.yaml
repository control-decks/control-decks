interface:
  display_name: "Endroit Context · Any Site Context"
  short_description: "Enumerate known Endroit Sites and select the unique semantic "
  default_prompt: "Use $endroit-context:endroit-context-any-site on the current working object."
policy:
  allow_implicit_invocation: false
