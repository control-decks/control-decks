interface:
  display_name: "Hairness Context · Any Target Context"
  short_description: "Enumerate known Hairness Targets and select the unique semantic "
  default_prompt: "Use $hairness-context:hairness-context-any-target on the current working object."
policy:
  allow_implicit_invocation: false
