interface:
  display_name: "Session Intents · Clear Main Session"
  short_description: "Clear the active Main Session coordination policy."
  default_prompt: "Use $session-intents:clear-main-session on the current working object."
policy:
  allow_implicit_invocation: false
