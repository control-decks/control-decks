interface:
  display_name: "Session Intents · Main Session"
  short_description: "Keep the current agent as a coordinator and delegate every bound"
  default_prompt: "Use $session-intents:main-session on the current working object."
policy:
  allow_implicit_invocation: false
