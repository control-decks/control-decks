interface:
  display_name: "Session Lifecycle · Rename Session"
  short_description: "Rename the current task from its resolved owner, axis, objective"
  default_prompt: "Use $session-lifecycle:rename-session on the current working object."
policy:
  allow_implicit_invocation: false
