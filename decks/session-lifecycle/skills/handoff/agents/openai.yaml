interface:
  display_name: "Session Lifecycle · Handoff"
  short_description: "Prepare or perform one resumable task handoff while preserving t"
  default_prompt: "Use $session-lifecycle:handoff on the current working object."
policy:
  allow_implicit_invocation: false
